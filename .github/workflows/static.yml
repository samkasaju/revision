name: Devops class start GitHub

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: |
          echo "hello world"
          hostname
          ip a
